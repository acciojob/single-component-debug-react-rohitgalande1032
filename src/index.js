import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

//setup with application with the help of id root
const root = document.getElementById('root')
ReactDOM.render(<App />,root)

